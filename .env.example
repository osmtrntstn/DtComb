# FastAPI Configuration
SECRET_KEY=9e222fe9-652f-4a63-afab-1909f76d4c5e
DEBUG=False

# Database Configuration
DATABASE_URL=sqlite:///./app/db/dtcomb_data.db

# Admin Credentials (Change these!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=adminPasword

# R Configuration
R_HOME=/usr/lib/R

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Docker Build Configuration
# ShinyProxy 3.2.0 uyumluluğu için BuildKit'i devre dışı bırakın.
# Bu ayar, image metadata alanlarının (parent, dockerVersion, author) doğru
# şekilde doldurulmasını sağlar.
# Kullanım: DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 docker-compose build
# veya: make docker-build
DOCKER_BUILDKIT=0
COMPOSE_DOCKER_CLI_BUILD=0
